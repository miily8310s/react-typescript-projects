const RepositoriesList = (): JSX.Element => {
  return (
    <div>
      <form action=''>
        <input type='text' />
        <button>Search</button>
      </form>
    </div>
  );
};

export default RepositoriesList;
